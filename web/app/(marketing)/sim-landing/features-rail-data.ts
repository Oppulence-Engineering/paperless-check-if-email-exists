import type { ProductPreviewKey } from "./subpages/solutions-product/product-previews";

/** One live product frame per homepage feature card — not the same PNG crop. */
export const FEATURE_RAIL_PREVIEW: Record<string, ProductPreviewKey> = {
	"commitment-register": "register",
	"account-mission-control": "account",
	"attention-queue": "web",
	"governed-actions": "govern",
};
