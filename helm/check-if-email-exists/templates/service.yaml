apiVersion: v1
kind: Service
metadata:
  name: {{ include "check-if-email-exists.fullname" . }}
  labels:
    {{- include "check-if-email-exists.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: backend
      protocol: TCP
      name: backend
    - port: {{ .Values.service.webPort }}
      targetPort: web
      protocol: TCP
      name: web
  selector:
    {{- include "check-if-email-exists.selectorLabels" . | nindent 4 }}
